# Phase 1 stub — replaced in Phase 4 with a real Scala/sbt build
# Keeps the container alive so docker compose up --build succeeds
FROM python:3.11-slim
WORKDIR /app

CMD ["python3", "-c", "import time; print('Stream Processor stub running — implement in Phase 4'); time.sleep(86400)"]

EXPOSE 8090
